sudo: required
language: rust
services:
  - docker

rust:
  - 1.34.0

before_install:
  - rustup self update

script:
  - make test
  # - make lint
  # - make build
  - travis_wait 50 make docker_build

deploy:
  - provider: releases
    api_key: $GITHUB_TOKEN
    on:
      tags: true
  - provider: script
    script: make docker_login && make docker_release
    on:
      tags: true

notifications:
  email:
    on_success: never
