package service

import (
	"context"

	"gitlab.com/bloom42/bloom/server/domain/users"
)

func (service *UsersService) UpdateUserProfile(ctx context.Context, params users.UpdateUserProfileParams) (ret users.User, err error) {
	return
}
