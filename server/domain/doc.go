// Package domain contains all the data structures and interfaces to represent the domain.
// Each subdomain (bounded context) is in a separate subdirectory.
// Each subdirectory contains the interfaces of the application services (use case layer) and the repositories (data access layer),
// the entities defintion.
// Each subdirectory of subdirectories contains the implementation of the repositories and of the
// services of the subdomains.
package domain
