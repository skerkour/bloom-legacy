package service

import (
	"context"

	"gitlab.com/bloom42/bloom/server/domain/billing"
)

func (service *BillingService) UpdatePlan(ctx context.Context, params billing.UpdatePlanParams) (ret billing.Plan, err error) {
	return
}
