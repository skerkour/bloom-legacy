package notification

const (
	// DEFAULT_SENDER_ADDRESS is the default email address of sender
	DEFAULT_SENDER_ADDRESS = "hello@bloom.sh"
	// DEFAULT_SENDER_NAME is the default name of the sender
	DEFAULT_SENDER_NAME = "Bloom"
)
