syntax = "proto3";

package com.bloom42.billing;
option go_package = "billing";

// Billing
service Billing {
  rpc CreatePlan(Empty) returns (Empty);
  rpc UpdatePlan(Empty) returns (Empty);
  rpc DeletePlan(Empty) returns (Empty);
}

message Empty {}
