import { Message as AuthMessage, Method as AuthMethod } from './auth';

type Method = AuthMethod;

type Message = AuthMessage;

// export default function call(_method: Method, _message: Message) {

// }
