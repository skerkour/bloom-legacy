package objects

import (
	"context"

	"gitlab.com/bloom42/bloom/cmd/bloom/server/domain/users"
)

// Pull is used to pull changes
func Pull(ctx context.Context, actor *users.User) (err error) {
	return
}
