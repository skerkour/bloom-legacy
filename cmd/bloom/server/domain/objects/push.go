package objects

import (
	"context"

	"gitlab.com/bloom42/bloom/cmd/bloom/server/domain/users"
)

// Push is used to push changes
func Push(ctx context.Context, actor *users.User) (err error) {
	return
}
